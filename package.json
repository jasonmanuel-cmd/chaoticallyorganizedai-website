{
  "name": "chaoticallyorganizedai-website",
  "version": "1.0.0",
  "private": true,
  "description": "Static HTML/CSS/JS site — no Node build step",
  "scripts": {
    "build": "echo Static deploy: no compile step"
  }
}
